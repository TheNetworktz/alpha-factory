# setup.py
from setuptools import find_packages, setup

setup(
    name='alpha_factory',
    packages=find_packages(),
    version='0.1.0',
    description='A modular quantitative research framework.',
    author='Tyler Ludwig', # Change this to your name
)
